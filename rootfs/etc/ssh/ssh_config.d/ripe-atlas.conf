# RIPE Atlas probe SSH configuration
#
# The registration servers use ssh-rsa host keys and may require
# ssh-rsa for client authentication. OpenSSH 8.8+ disabled ssh-rsa
# (SHA-1) by default, so we need to re-enable it.

Host *
    PubkeyAcceptedAlgorithms +ssh-rsa
    HostkeyAlgorithms +ssh-rsa
